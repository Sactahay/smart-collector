services:
  - type: web
    name: smart-collector-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate --noinput
    startCommand: python manage.py create_default_vehicle && gunicorn smart_collector.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
      - key: DJANGO_SETTINGS_MODULE
        value: smart_collector.settings
      - key: WEB_CONCURRENCY
        value: 3
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: "smartcollectorolintepeque.com,www.smartcollectorolintepeque.com,smart-collector-backend.onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: smart-collector-db
          property: connectionString

  - type: static
    name: smart-collector-frontend
    buildCommand: |
      npm install
      npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_API_URL
        value: "https://smart-collector-backend.onrender.com/api"

databases:
  - name: smart-collector-db
